buildscript {
    repositories {
        // Chỉ giữ những repository cần thiết cho buildscript
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.1'
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
